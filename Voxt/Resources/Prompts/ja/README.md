# Japanese Prompts

Japanese prompt templates used as localized defaults for Voxt features.

## Responsibilities

- Provides Japanese default text for rewrite, translation, dictionary, note, and meeting prompt flows.
- Keeps localized prompt copy reviewable as resource files.
- Mirrors the shared prompt resource naming used by other locales.
