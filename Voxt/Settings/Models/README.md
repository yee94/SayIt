# Model Settings

Settings UI for local and custom model catalog, downloads, installation state, and generation options.

## Responsibilities

- Presents model catalogs, selection state, download progress, badges, and install actions.
- Builds model snapshots from managers and provider data for stable UI rendering.
- Keeps model settings lifecycle, observation, and actions scoped to model-management screens.
